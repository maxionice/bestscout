# FM26 Steam build 23583635 acceptance record

Runtime acceptance was performed on Linux/Wayland with Proton Experimental on
2026-07-21. The executable, `GameAssembly.dll` and IL2CPP metadata fingerprints
all matched profile `fm26-steam-23583635`.

Verified behavior:

- BestScout selected the real Wine `fm.exe` process instead of the Steam,
  pressure-vessel, Python and Proton launcher processes.
- `/proc/<pid>/maps` exposed mapped bases for `fm.exe` and `GameAssembly.dll`.
- The bounded read-only process API read exactly two bytes from the executable
  base and confirmed the `MZ` PE signature.
- BepInEx 6 was active, but no BestScout bridge descriptor was present. Domain
  reads and all editor writes therefore remained disabled.
- No game-memory write API was present or used during acceptance.

This record approves process inspection only. It does not approve domain roots,
field layouts or writes; those require separate versioned acceptance evidence.
